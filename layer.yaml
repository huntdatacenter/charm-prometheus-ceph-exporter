includes: ['layer:basic', 'interface:http', 'layer:snap', 'interface:ceph-client']
repo: 'https://git.launchpad.net/prometheus-ceph-exporter-charm'
ignore: ['.*.swp' ]
options:
  basic:
    use_venv: true
    include_system_packages: true
  snap:
    prometheus-ceph-exporter:
      channel: stable
